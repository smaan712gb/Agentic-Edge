"""Morning Report — the daily CIO brief.

Assembles what the always-on sweeps already persist (theme runs, exit
pressure, rotation, EDGAR stake watch, 13F deltas, insider alerts) into one
read-only report. Decision-support only: nothing in this package trades or
gates a trade.
"""
