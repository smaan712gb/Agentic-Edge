# ── Web / API framework ───────────────────────────────────────────────
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.0

# ── Persistence + scheduling (API layer) ──────────────────────────────
sqlalchemy[asyncio]>=2.0
aiosqlite>=0.20          # async driver for the default dev SQLite DB
alembic>=1.13            # migrations: `alembic -c api/alembic.ini upgrade head`
apscheduler>=3.10
# asyncpg>=0.29          # uncomment for Postgres instead of SQLite

# ── HTTP + TLS (TLS verified against the OS trust store for corp proxies) ─
httpx>=0.27
truststore>=0.10

# ── Brokerage (paper / live via IB Gateway) ───────────────────────────
ib_insync>=0.9.86

# ── Agent + provider engine ───────────────────────────────────────────
# The reasoning agents (analyst pod, bull/bear debate, research manager,
# trader, risk committee) and all data-provider clients live in the
# `tradingagents` package — forked and substantially enhanced from the
# open-source TradingAgents framework (Apache-2.0). This pulls the
# importable package + its dependencies. To customize the engine instead,
# clone https://github.com/smaan712gb/TradingAgents and `pip install -e .`.
#
# PINNED TO A COMMIT, NOT A BRANCH. This package places the actual orders —
# leg selection, the walking-limit executor, the broker provider, the macro
# guardrail. A floating `@agentic-edge` meant two installs a week apart could
# run different execution code against the same account, with nothing in this
# repo's history recording which pair was ever tested together.
#
# It also broke silently: on 2026-08-17 the branch HEAD lacked the LEAPS-only
# eligibility fix that entry_loop calls via `select_pmcc_legs(...,
# require_short_call=...)`. A rebuild elsewhere would have placed ZERO trades
# and looked like a data problem, not a dependency problem.
#
# When bumping: push the engine first, then update this SHA in the same commit
# as any API-side change that depends on it, so the pair moves together.
# 9c43d4a = macro-guardrail FMP fallback + 270-720d LEAP window + OI floor 10 (2026-08-17)
tradingagents @ git+https://github.com/smaan712gb/TradingAgents.git@9c43d4aaf10fb3e78706bb1c8856ea0c8f716ef6
